<div class="profile">

<div class="profile-heading">
  <div class="container">
    <div class="row">
      <div class="col-md-10 col-md-offset-1">
        <!-- <div class="avatar">
          <% if current_user.avatar_url.present? %>
            <%= image_tag current_user.avatar_url, class: "img-circle" %>
          <% else %>
            <%= image_tag gravatar_url(current_user), class: "img-circle" %>
          <% end %>
        </div> -->
        <h3>Below is a listing of all the users that have created blox to be a part of the Weblox project. Check out each user's blox!</h3>
      

      </div>
    </div>
  </div>
</div>
<div class="profile-border" width="300" height="200"></div>
  
  <div class="container"><div class="row"><div class="col-md-10 col-md-offset-1">
  <div class="profile-listing">
    <% @all_users.each do |i| %>
      <% user = User.find(i) %>
        <div class="item">
          <div class="item-content">
            <li class='col-md-2 col-sm-4 col-xs-6 builder-profile'> 
            <%= link_to profile_path(user.profile.id), class: "avatar" do %>    
              <div class="avatar">
                  <% if user.profile.avatar.present? %>
                    <%= image_tag user.profile.avatar.url(:thumb), class: "img-circle"%>
                  <% elsif user.avatar_url.present? %>
                    <%= image_tag user.avatar_url, class: "img-circle" %>
                  <% else %>
                    <%= image_tag gravatar_url(user), class: "img-circle" %>
                  <% end %>
              </div>
              <% end %>
              <div>
                <%= link_to user.profile.handle, profile_path(user.profile.id) %>
              </div>
            </li>
          <% end %>

      </div>
    </div>
  </div>
  </div></div></div>


</div>
  
<!-- <br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<div>
<h1>Listing profiles</h1>

<table class="table">
  <thead>
    <tr>
      <th>Handle</th>
      <th>Company</th>
      <th>Bio</th>
      <th>Website</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @profiles.each do |profile| %>
      <tr>
        <td><%= profile.handle %></td>
        <td><%= profile.company %></td>
        <td><%= profile.bio %></td>
        <td><%= profile.website %></td>
        <td><%= profile.user %></td>
        <td><%= link_to 'Show', profile %></td>
        <td><%= link_to 'Edit', edit_profile_path(profile) %></td>
        <td><%= link_to 'Destroy', profile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Profile', new_profile_path %>
</div> -->

